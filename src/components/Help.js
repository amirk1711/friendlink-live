import React from 'react';

function Help(props) {
    return (
        <div className="setting-actions">
            <div className="help-page">Not created yet.</div>
        </div>
    );
}

export default Help;